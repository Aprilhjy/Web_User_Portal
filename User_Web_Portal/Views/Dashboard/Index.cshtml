
@{
    ViewBag.Title = "Index";
}

<h2>Dashboard</h2>

<p>Welcome <b>@WebSecurity.CurrentUserName</b> - <a href='@Url.Action("LogOut", "Account")'>Log out</a></p>

<p>
    @if (User.IsInRole("Administrator"))

    {
        @Html.ActionLink("Register", "Register", "Account") <span> || </span>
    }

    @Html.ActionLink("Change Password", "ChangePassword", "Account") <span> || </span>

    @Html.ActionLink("User Profile", "UserProfileModel", "Account") <span> || </span>


</p>